while 的出口是一个需要谨慎考虑的问题，我们可以从迭代器的思维去简化这里的心智模型

迭代器每次“检查是否还有剩余数据”，这要求：
1. 迭代器指针初始指向 null (可以理解为 -1 )，第一次next 时指向第一行
2. 迭代器的next()只要返回true，本次 while 一定要完全结束

## 结论
- 实现一个迭代器，游标应当指向 -1 （数组）或 null （对象游标）或dummy （链表）
- next() 方法里要去判断游标的下个状态是否存在
-
