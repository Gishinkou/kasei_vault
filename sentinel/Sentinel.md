## 命名溯源 - Sph
核心类都包含字符串`"Sph"`，根据github issues 里的官方回答，它的意思是 `Semaphore`，也就是操作系统里的**信号量**概念。
在限流熔断等功能的组件中，底层思想都包含"计数+阻塞控制"，与操作系统信号量有一定的关联。

> Semaphore = 带计数的并发控制器，用于限制资源同时访问数量
## 入口API
### `SpuU` 提供 try-catch 风格API

